# Disabled until performance regression gates are reintroduced.
name: CodSpeed

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  benchmarks:
    name: Benchmarks (deferred)
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Note
        run: echo "CodSpeed benchmarks are deferred. Re-enable when performance gates return."
